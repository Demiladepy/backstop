# Local development — copy to .env.local and point at your Convex dev deployment.
VITE_CONVEX_URL=https://your-dev-deployment.convex.cloud

# Convex CLI (optional for local `npx convex dev`)
# CONVEX_DEPLOYMENT=dev:your-dev-deployment
