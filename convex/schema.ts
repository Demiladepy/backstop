import { defineSchema } from 'convex/server'

// P0 intentionally defines no product tables. Add tables only alongside the
// validated workflows and authorization rules that use them.
export default defineSchema({})
